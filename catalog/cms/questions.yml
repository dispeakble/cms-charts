namespace: {{ .Values.global.namespace | default }}
questions:
- variable: redis.host
  default: "{{ .Values.global.redis.host }}"
  description: "Specify the Redis host"
  type: string
  label: Redis host
  group: "Redis"
- variable: redis.port
  default: "{{ .Values.global.redis.port }}"
  description: "Specify the Redis port"
  type: string
  label: Redis port
  group: "Redis"